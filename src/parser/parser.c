#include "../generic_array.h"
#include "../structure.h"
#include "parser.h"

Retval populate_structure(
        Structure *structure,
        const ARRAY_NAME(Token) *tokens
) {
        return GOOD;
}
